# Stewart Bradley Pier Services Website

Static website for GitHub Pages.

## Publish
1. Replace the files in the existing GitHub repository with this folder's contents.
2. Commit to the branch configured for GitHub Pages (normally `main`).
3. Keep the `CNAME` file in the repository root.

The estimate form uses the existing Formspree endpoint. Confirm the endpoint remains active after publishing.
